# `assets/source/`

Authoring files. Vector originals, layered art, audio projects, animation source.

Not imported by Godot and not shipped. Export to `assets/runtime/` for anything the
game loads.

Current contents retain art-generation records and the audio generator's source
policy. Runtime exports and their exact manifests live under `assets/runtime/`.
